/**
 * Database provider types which are used when injecting database connections.
 * They represent different types of databases we connect to
 */
export const DatabaseProviderTypes = {
  MainDatabase: 'MainDatabase',
};
