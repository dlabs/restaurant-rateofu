/**
 * Database repository provider types which are used when injecting repositories
 */
export const RepositoryProviderTypes = {
  OrderRepository: 'OrderRepository',
  OrderItemRepository: 'OrderItemRepository',
  MenuItemRepository: 'MenuItemRepository',
};
